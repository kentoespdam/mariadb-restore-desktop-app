// Tailwind v4 is configured via @import in style.css. This file
// exists so tools that probe for a config (e.g. shadcn add) find one.
export default {
  content: ['./src/**/*.{ts,tsx,js,jsx,html}'],
  theme: { extend: {} },
  plugins: [],
};
