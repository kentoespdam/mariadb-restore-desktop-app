# Claim Order — drop-shadcn

Created: 2026-08-29 · Source: grill-with-docs session

Per `knowledge.md` grill mode this plan ships only docs; the implementation is one beads issue that any agent can pick up in coding mode. Claim with `bd update <ID> --claim` and close with `bd close <ID>`. No blockers; this is the first slice of the plan and can start immediately.

- [ ] bd-lre — drop shadcn: rewrite `Profiles.tsx` with raw Tailwind, add `Button.tsx`, remove shadcn components + dead deps — can start immediately
