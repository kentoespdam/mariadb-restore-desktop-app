export function Dashboard() {
  return (
    <section>
      <h2 className="text-xl font-semibold">Dashboard</h2>
      <p className="mt-2 text-slate-400">
        Layout bootstrap ready. Backup, Restore, and Settings land in the v1 follow-up.
      </p>
    </section>
  );
}
